# 5.2 리서치 단계

> 서브에이전트를 활용하여 코드베이스를 깊이 분석하고, 결과를 research.md 마크다운 파일로 작성합니다.

> 🎯 **이 섹션에서 배울 Claude Code 기능**: 서브에이전트 (코드베이스 분석), 마크다운 파일 기반 결과 정리

> ⭐⭐ **난이도**: 보통

## 학습 목표

- Claude에게 코드를 "깊이", "세부 사항까지" 읽도록 요청하는 방법을 배웁니다
- 결과를 구두 요약이 아닌 마크다운 파일로 작성시키는 이유를 이해합니다
- research.md를 검토하고 부정확한 점을 수정 요청하는 방법을 익힙니다

## 왜 리서치 단계가 필요한가?

복잡한 기능을 구현하기 전에 Claude가 기존 코드베이스를 정확히 이해하지 못하면:

- 기존 쿼리 패턴을 무시하고 새로운 방식으로 구현
- 이미 있는 유틸리티 함수를 중복 생성
- UI 컴포넌트 스타일이 기존과 불일치
- 서버/클라이언트 분리 패턴을 위반

리서치 단계에서는 Claude에게 코드베이스를 **깊이(deeply)**, **세부 사항까지(in great details)** 읽도록 명시적으로 요청합니다.

{% hint style="tip" %}
Boris Tane 워크플로의 핵심 통찰: Claude에게 "분석해줘"라고만 하면 얕은 분석이 나옵니다. **"깊이"**, **"세부 사항까지"** 같은 수식어를 명시적으로 사용하면 더 철저한 분석을 얻을 수 있습니다.
{% endhint %}

## 실습: Todo 앱 코드베이스 리서치

### Step 1: 서브에이전트로 기존 코드베이스 분석

서브에이전트를 사용하여 기존 Todo 앱의 코드 패턴을 분석합니다. 서브에이전트는 별도 컨텍스트에서 실행되므로 메인 대화를 오염시키지 않습니다.

→ *이 프롬프트는 서브에이전트에게 기존 코드 패턴을 깊이 분석시켜, 통계 대시보드를 기존 코드와 일관되게 만들 수 있는 정보를 수집하기 위한 것입니다:*

```
서브에이전트를 사용해서 이 프로젝트의 코드베이스를 깊이 분석해줘. 다음 항목을 세부 사항까지 조사해줘:

1. 데이터베이스 쿼리 패턴:
   - lib/queries/ 디렉토리의 모든 쿼리 함수
   - Drizzle ORM 사용 방식 (select, where, join, aggregate 등)
   - 함수 시그니처와 반환 타입

2. UI 컴포넌트 패턴:
   - 사용 중인 shadcn/ui 컴포넌트 목록
   - 레이아웃 패턴 (카드, 그리드, 스페이싱)
   - Tailwind CSS 클래스 사용 규칙

3. Server Action 구조:
   - actions/ 디렉토리의 모든 액션 함수
   - Zod 유효성 검사 패턴
   - 에러 처리 방식

4. 데이터 흐름:
   - 서버 컴포넌트 → 쿼리 함수 → 데이터베이스 패턴
   - 클라이언트 컴포넌트에서 서버 데이터 사용 방식
   - revalidatePath 사용 패턴

5. 라우팅 구조:
   - app/ 디렉토리의 라우트 구조
   - 네비게이션 패턴

통계 대시보드를 기존 패턴과 일관되게 만들 수 있도록 충분히 상세하게 조사해줘.
```

Claude Code가 서브에이전트를 생성하여 코드베이스를 분석합니다. 서브에이전트는 읽기 전용 도구만 사용하므로 코드를 변경하지 않습니다.

{% hint style="info" %}
**서브에이전트 복습**: Chapter 2.7에서 배운 서브에이전트는 별도 컨텍스트에서 실행됩니다. 탐색 결과가 메인 대화를 차지하지 않아 컨텍스트를 효율적으로 관리합니다.
{% endhint %}

### Step 2: research.md 파일 생성 요청

서브에이전트의 분석이 끝나면, 결과를 **마크다운 파일**로 정리하도록 요청합니다.

→ *이 프롬프트는 리서치 결과를 검토 가능한 마크다운 파일로 저장하여, 구두 요약이 아닌 체계적인 분석 자료를 확보하기 위한 것입니다:*

```
분석 결과를 research.md 파일로 작성해줘. 다음 섹션을 포함해줘:

1. ## 기존 쿼리 패턴
   - 각 쿼리 함수의 시그니처, 역할, 사용 위치
   - 집계(aggregate) 쿼리 사용 여부
   - 트랜잭션 사용 패턴

2. ## 사용 가능한 UI 컴포넌트
   - 이미 설치된 shadcn/ui 컴포넌트 목록
   - 추가 설치가 필요할 수 있는 컴포넌트 (차트 등)
   - 현재 사용 중인 Tailwind 패턴

3. ## DB 스키마 관계도
   - todos, categories 테이블의 필드 목록과 타입
   - 테이블 간 관계 (외래 키)
   - 통계에 활용할 수 있는 필드 (completed, priority, dueDate, createdAt 등)

4. ## 서버/클라이언트 분리 패턴
   - 현재 서버 컴포넌트가 데이터를 패칭하는 방식
   - 클라이언트 컴포넌트에 데이터를 전달하는 방식
   - "use client" 사용 규칙

5. ## 통계 대시보드 구현 시 고려사항
   - 기존 패턴을 따르기 위해 주의할 점
   - 새로 필요한 쿼리 함수 목록 (예상)
   - 차트 라이브러리 필요 여부

research.md는 프로젝트 루트에 생성해줘.
```

### 왜 파일로 작성하는가?

| 방식 | 장점 | 단점 |
|------|------|------|
| **구두 요약** (대화 내) | 빠름 | 지나가면 사라짐, 라인 단위 검토 불가 |
| **파일 작성** (research.md) | 검토 가능, 수정 요청 가능, 추후 참조 가능 | 파일 관리 필요 |

파일로 작성하면:
- **라인 단위로 검토** 가능 — "3번 항목이 틀렸어, 실제로는..."
- **에디터에서 열어** 직접 주석 추가 가능
- **다음 단계(계획)에서 참조** — Claude가 research.md를 읽고 계획에 반영
- **여러 세션에 걸쳐** 사용 가능 — `/clear` 후에도 파일은 남아 있음

### Step 3: research.md 검토 및 수정 요청

생성된 research.md를 에디터에서 열어 검토합니다. 부정확하거나 빠진 내용이 있으면 수정을 요청합니다.

→ *이 프롬프트는 리서치 결과에서 발견된 부정확한 부분을 수정하여 정확한 기반 자료를 만들기 위한 것입니다:*

```
research.md를 검토했어. 다음 사항을 수정해줘:

1. "기존 쿼리 패턴" 섹션에서 getCategories 함수의 반환 타입이 틀려. 실제로는 {id, name, color}[] 형태야.
2. "사용 가능한 UI 컴포넌트" 섹션에 Badge 컴포넌트가 빠져있어. 카테고리 표시에 사용하고 있어.
3. "통계 대시보드 구현 시 고려사항" 섹션에 차트 라이브러리 없이 Tailwind CSS만으로 간단한 바 차트를 만드는 방안도 추가해줘.
```

{% hint style="warning" %}
**중요**: research.md의 정확성이 이후 단계의 품질을 결정합니다. 시간을 들여 꼼꼼히 검토하세요. 특히 쿼리 함수 시그니처, 컴포넌트 목록, 테이블 스키마가 실제 코드와 일치하는지 확인하세요.
{% endhint %}

### research.md 예시

Claude가 생성하는 research.md는 다음과 같은 구조를 가집니다:

```markdown
# Research: Todo 앱 코드베이스 분석

## 기존 쿼리 패턴

### lib/queries/todos.ts
- `getTodos()`: 모든 할 일 목록 조회, categories와 LEFT JOIN
- `getTodoById(id: string)`: 단일 할 일 조회, 카테고리 포함
- `getFilteredTodos(filters)`: 필터링된 할 일 조회

### lib/queries/categories.ts
- `getCategories()`: 모든 카테고리 목록 조회
- 반환 타입: `{ id: string; name: string; color: string }[]`

### 쿼리 패턴 요약
- Drizzle ORM의 select().from().where() 체이닝 패턴 사용
- LEFT JOIN으로 관련 테이블 연결
- 집계 쿼리는 현재 사용하지 않음 (통계에서 새로 필요)

## 사용 가능한 UI 컴포넌트

### 설치된 shadcn/ui 컴포넌트
- Button, Input, Textarea, Select
- Card, CardHeader, CardTitle, CardContent
- Badge, Dialog, Calendar
- Table, TableHeader, TableRow, TableCell

### Tailwind 패턴
- 카드: `rounded-lg border bg-card p-6`
- 그리드: `grid grid-cols-1 md:grid-cols-3 gap-4`
- 스페이싱: `space-y-4`, `space-y-6`

## DB 스키마 관계도

todos ──── categoryId ────► categories
 │                            │
 ├─ id (text, PK)             ├─ id (text, PK)
 ├─ title (text)              ├─ name (text)
 ├─ description (text)        ├─ color (text)
 ├─ completed (boolean)       └─ createdAt (timestamp)
 ├─ priority (text: high/medium/low)
 ├─ dueDate (timestamp)
 ├─ categoryId (text, FK)
 ├─ createdAt (timestamp)
 └─ updatedAt (timestamp)

## 서버/클라이언트 분리 패턴
- 페이지(page.tsx): 서버 컴포넌트, 쿼리 함수 직접 호출
- 폼 컴포넌트: "use client", Server Actions 사용
- 데이터 전달: 서버 → 클라이언트는 props로 전달

## 통계 대시보드 구현 시 고려사항
- 집계 쿼리 함수 새로 생성 필요 (COUNT, GROUP BY)
- 차트 라이브러리 또는 Tailwind CSS 기반 커스텀 차트
- 서버 컴포넌트에서 데이터 패칭 → 클라이언트 차트 컴포넌트에 전달
```

## 리서치 단계 팁

### 1. "깊이" 수식어를 명시적으로 사용하기

| 얕은 분석 요청 | 깊은 분석 요청 |
|-------------|-------------|
| "코드를 분석해줘" | "코드를 **깊이(deeply)** 분석해줘" |
| "패턴을 요약해줘" | "패턴을 **세부 사항까지(in great details)** 요약해줘" |
| "구조를 확인해줘" | "구조를 **모든 파일을 포함해서** 확인해줘" |

### 2. 분석 항목을 구체적으로 나열하기

"코드를 분석해줘"보다 위 프롬프트처럼 5가지 항목을 명시적으로 나열하면 Claude가 누락 없이 분석합니다.

### 3. research.md는 완벽하지 않아도 됨

첫 번째 버전이 100% 정확할 필요는 없습니다. 검토 후 수정 요청을 통해 점진적으로 정확도를 높이면 됩니다. 중요한 것은 **검토 가능한 형태로 정보를 확보**하는 것입니다.

## 핵심 정리

| 개념 | 설명 |
|------|------|
| 리서치 단계 | 코드 작성 전 기존 코드베이스를 깊이 분석하는 단계 |
| 서브에이전트 활용 | 별도 컨텍스트에서 코드 분석, 메인 대화 오염 방지 |
| research.md | 분석 결과를 마크다운 파일로 저장, 검토 가능한 표면 확보 |
| 깊이 수식어 | "deeply", "in great details" 등을 명시적으로 사용 |
| 검토 → 수정 | research.md를 확인하고 부정확한 부분 수정 요청 |

---

## ✅ 체크포인트

이 섹션을 완료하면 다음을 확인하세요:

- [ ] 서브에이전트를 사용하여 코드베이스 분석을 수행함
- [ ] research.md 파일이 프로젝트 루트에 생성됨
- [ ] research.md의 내용이 실제 코드와 일치하는지 검토함
- [ ] 부정확한 부분에 대해 수정 요청을 수행함

---

> **다음**: [5.3 계획 수립 단계](03-planning-phase.md)에서 research.md를 기반으로 상세한 구현 계획을 plan.md에 작성합니다.
